{{#each charas as |chara|}}
<div class="row mb-4 chara">
  <div class="col-md-5">
    <h2>
      <a href="/chara/{{chara.id}}">{{chara.name}}</a>
    </h2>
    <p>Created by {{chara.user.name}}</p>
  </div>
  <div class="col-md-7">
    <p>
      {{chara.desc}}
    </p>
  </div>
</div>
{{/each}}